- Was wondering why put was inconsistenly appending to the file turns out you need to flush the filestream, until you flush everything is added to a buffer which is only flushed upon a few conditions like program exit. My assertions failing meant the program wasn't exiting thus the OS hadn't actually seen any changes in the size of the file yet.